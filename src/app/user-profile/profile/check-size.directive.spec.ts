import { CheckSizeDirective } from './check-size.directive';

describe('CheckSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckSizeDirective();
    expect(directive).toBeTruthy();
  });
});
